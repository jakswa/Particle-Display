Working on particle display for Dr. Heller.

Just getting display to work at first.

To compile (gtkmm required):  
g++ -o test main.cc myarea* `pkg-config gtkmm-2.4 --cflags --libs`


git push steps
==============
touch README
git add README
git commit -m 'first commit'
git remote add origin git@github.com:jakswa/asdf.git
git push origin master
